import React from 'react';
import { Route } from 'react-router-dom';
import isLogin from '../Utilities/IsLogin'

function PublicRoute({componet: Component, ...rest}) {
    return (
        <Route 
        
        />
    );
}

export default PublicRoute;